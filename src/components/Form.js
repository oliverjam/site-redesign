import styled from 'styled-components';

export const Form = styled.form``;

export const Input = styled.input`
  padding: ${p => p.space[2]};
  border: none;
  border-bottom: 1px solid;
`;

export const Label = styled.label`font-size: 0.85rem;`;
